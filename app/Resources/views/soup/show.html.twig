{% extends 'base.html.twig' %}

{% block body %}
    <h1>Soup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ soup.name }}</td>
            </tr>
            <tr>
                <th>Litr</th>
                <td>{{ soup.litr }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_soup_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_soup_edit', { 'id': soup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
