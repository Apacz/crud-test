{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dishes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dish in dishes %}
            <tr>
                <td><a href="{{ path('admin_dish_show', { 'id': dish.id }) }}">{{ dish.id }}</a></td>
                <td>{% if dish.date %}{{ dish.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_dish_show', { 'id': dish.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_dish_edit', { 'id': dish.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_dish_new') }}">Create a new dish</a>
        </li>
    </ul>
{% endblock %}
